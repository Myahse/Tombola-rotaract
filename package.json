{
  "name": "club-tombola",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n api,web,org,mail,admin -c magenta,cyan,yellow,green,blue \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix organizer\" \"npm run dev --prefix campaign\" \"npm run dev --prefix admin\"",
    "db:seed": "npm run db:seed --prefix backend",
    "build": "node scripts/vercel-build.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
